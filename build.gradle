apply plugin: 'java'

sourceSets {
  main {
    java { srcDir 'java' }
  }
}

repositories {
    mavenCentral()
}

dependencies {
  compile 'com.google.javascript:closure-compiler:r1592'
  compile 'javax.servlet:javax.servlet-api:3.0.1'
}
